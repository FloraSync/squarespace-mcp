# Local stdio transport
SQUARESPACE_API_KEY=

# Remote HTTP transport
MCP_PUBLIC_URL=https://your-service.example/mcp
MCP_TOKEN_SECRET=

# Safe by default. Set false or use --read-write to enable write tools.
SQUARESPACE_MCP_READ_ONLY=true
